# libthe-seed Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-12

## Active Technologies

- C++20 (matches existing `libthe_seed_la_CXXFLAGS = -std=c++20`) + None new — the entire point is removing LIEF. Uses only standard library + existing project deps (ecs-cpp, pthread) (001-dependency-lister)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for C++20 (matches existing `libthe_seed_la_CXXFLAGS = -std=c++20`)

## Code Style

C++20 (matches existing `libthe_seed_la_CXXFLAGS = -std=c++20`): Follow standard conventions

## Recent Changes

- 001-dependency-lister: Added C++20 (matches existing `libthe_seed_la_CXXFLAGS = -std=c++20`) + None new — the entire point is removing LIEF. Uses only standard library + existing project deps (ecs-cpp, pthread)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
